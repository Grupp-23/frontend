import "./GameBase.css"
import Base from "../assets/Base.png";

function GameBase(){
    return(
    
        <div id = "BaseView">
            <img src = {Base} />
        </div>

    );
}

export default GameBase;